using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class OnStatusMaxMin {
	
	//references

	public void ExecuteHundredPercentEffect(BaseStatusEffect statusExtra)
	{
		switch(statusExtra.StatusEffectName){
			case Bleed:
				//unconscious  ==> maxHP harm, death, clotter;
			case Poisoned:
				//death;
			case Poisoned :
				//unconscious ==> resilience debuff, Darkened Blood;
			case Stinking :
				//gain True Scourge Stench Perk, remove Stinking;
			
	}
	public void ExecuteAtRemoval(BaseStatusEffect statusExtra)
	{
		switch(statusExtra.StatusEffectName){
			case Flying:
				//gain Confused(concussed), receive damage. If perk, nothing;
			case Stun:
				//if perk, then restore small% hp and wp;
			case Primed:
				//apply impotent status. if perk, 70% apply impotent status, 10% apply Primed status, 20% apply nothing;
			case Stinking:
				//if perk, then wp damage;
	}
	
	public void ExecuteAtApplication(BaseStatusEffect statusExtra)
	{
		switch(statusExtra.StatusEffectName){
			case Blind:
				//if perk, then damage buff;
			case Bleeding:
				//if perk, then agility buff;
			case :
				//;
			case :
				//;
	}
}
*/
