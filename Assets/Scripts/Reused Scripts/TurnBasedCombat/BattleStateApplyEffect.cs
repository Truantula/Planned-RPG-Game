using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BattleStateApplyEffect {

	/*public GameObject newStatus;
	public bool isAfflicted;
	public bool willAbilityHit;
	public void CheckAbilityForStatusEffects(BaseAbility usedAbility)
	{
		GameObject BattleManager = GameObject.Find("BattleManager");
		BattleGUI battleGUI = BattleManager.GetComponent<BattleGUI>();
		if (usedAbility.AbilityStatusEffects.Count > 0)
		{
			newStatus = possibleTarget.transform.Find("statusIcons").gameObject;
			StatusManager statusManager = newStatus.GetComponent<StatusManager>();
			if (statusManager.internalManager.statusQueue.Contains(InternalStatusManager.StatusType.BLEEDING))
			{
				Debug.Log("Already Bleeding");
			}
			else
			{
				statusManager.internalManager.statusQueue.Add(InternalStatusManager.StatusType.BLEEDING);
				statusManager.ChangeIcons();
				Debug.Log("Apply status");
			}
		}
		else 
		{
			//StateMachine.currentState = StateMachine.BattleStates.CALCDAMAGE;
		}
	}

	private void ApplyEffect(BaseAbility usedAbility)
	{
		//int randomTemp = RandomRange ()
	}*/
}
